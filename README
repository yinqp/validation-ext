# 扩展 hibernate-validation验证


**支持在实体类上验证多个字段


## @LeastOne 至少一个不能为空

同一个类中或同一个类中不同字段或方法上，至少一个值不为空

## @Same 标注的字段必须一样
同一个参数接收实体类上的字段必须一样

## @SameNo 标注字段必须不一样

同一个参数接受实体类上的字段值必须不一样

## @DateTimeString
字段必须符合固定格式的日期格式化
